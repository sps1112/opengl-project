#ifndef ENTITY_H
#define ENTITY_H

#include <Primitive.h>
#include <Shader.h>
#include <Light.h>
#include <Model.h>

/*union EntityObject
{
    Primitive prm;
    Light light;
    Model model;
    Shader shader;
};

enum ENTITY_TYPE
{
    PRIMITIVE_OBJECT,
    LIGHT_OBJECT,
    MODEL_OBJECT,
    SHADER_OBJECT,
};

class Entity
{
public:
    EntityObject object;
    ENTITY_TYPE type;
    Entity();

private:
}*/

#endif